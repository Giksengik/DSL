package parser

import lexer.Token
import java.util.*

object IfProcessor : StructureProcessor {

    override fun processStructure(notProcessedTokens: List<Token>, stack: Stack<Token>, postfix: MutableList<Token>) {
        TODO("Not yet implemented")
    }
}